---

- hosts: localhost
  connection: local
  become: true
  roles:
    #- role: base
      #tags: base
    - role: dotfiles
      tags: dotfiles
    #- role: pyenv
      #tags: pyenv
  #roles:
      #- {role: 'base', tags: 'base'}
      #- {role: 'dotfiles', tags: 'dotfiles'}
