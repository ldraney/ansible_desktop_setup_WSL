---

#I think this one is working
- name: update
  ansible.builtin.apt:
    update_cache: yes
##This definitely isn't working
#- name: upgrade
  #ansible.builtin.apt:
    #only_upgrade: yes

- name: install packages round 1
  tags:
    - base
  apt:
    update_cache: yes
    pkg: 
      - tmux
      - zsh
      #- xclip
